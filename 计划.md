# 算法负责人双版本实施计划

## 执行进度（实时更新）

- [x] 项目骨架、输入适配与固定版概率推理
- [x] 风险感知 A*、期望效用矩阵与约束任务分配
- [x] 四类动态事件与重规划日志
- [x] 半人工合成数据与概率层监督学习
- [x] CLI、标准场景、JSON Schema、训练产物与双版本输出
- [x] 全量测试、端到端验收与最终交接

当前交接详情见 `HANDOFF.md`。

## 摘要

- 目标：在 `2026-06-16` 晚上前，交付你负责的算法部分，做出两版可对比方案：
  - `版本 A：不可学习版`，使用固定参数与专家规则。
  - `版本 B：可学习版`，保持同样的公式结构，但用监督学习自动学习权重。
- 主链路统一为：`结构化输入 JSON → 贝叶斯/概率推理 → 生命风险与优先级 → 期望效用 → 任务分配 → 风险感知 A* → 动态重规划`
- 答辩亮点：同一套系统、同一套输入输出、两套参数来源，对比“专家知识驱动”与“数据驱动校准”。

## 双版本设计

- `版本 A：不可学习版`
  - 使用项目书中的固定线性权重。
  - 作为系统基线，保证稳定、可解释、容易展示。
  - 公式直接写死在配置中，可放到 `config.weights`。
- `版本 B：可学习版`
  - 不改变模块结构，只学习权重参数。
  - 学习对象分两组：
    - `trapped_prob` 权重：`sos_signal`, `building_collapse`, `human_activity`, `smoke`
    - `passability_prob` 权重：`road_damage`, `fire_risk`, `congestion`, `drone_confidence`
  - 训练方式：监督学习。
  - 输出结果：生成一组学习后的权重，替换固定版配置，再走同一条决策流水线。
- 明晚前不做的内容
  - 不学习完整任务分配策略。
  - 不学习完整 A* 路径策略。
  - 不构建复杂贝叶斯网络 CPT 学习。
  - 不做在线增量学习。

## 关键模块

- `input_adapter`
  - 接收 Qwen 输出或预设 `scenario_input.json`。
  - 校验、补全默认字段、统一成标准场景对象。
- `inference_engine`
  - 不可学习版：直接按固定权重计算。
  - 可学习版：加载训练得到的权重后计算。
  - 输出：`trapped_prob`, `passability_prob`, `life_risk`, `priority_score`
- `utility_engine`
  - 基于概率结果、时间代价、路径风险生成 `utility_matrix`
  - 两个版本共用同一套公式，只是上游概率来源不同
- `allocator`
  - 根据效用矩阵和约束做任务分配
  - 明晚前建议采用“约束过滤 + 贪心/枚举”方式
- `risk_aware_astar`
  - 用固定风险权重做 A*
  - 输出每个单位的 `path`, `eta`, `path_risk`
- `dynamic_replanner`
  - 至少支持 `road_collapse` 和 `drone_update`
  - 事件触发后重算概率、分配、路径

## JSON 规范

- 文件名建议：`scenario_input.json`
- 顶层结构：

```json
{
  "scenario_id": "demo_case_01",
  "generated_at": "2026-06-15T20:00:00+08:00",
  "command_center": {"node_id": "HQ"},
  "hospital": {"node_id": "HOSPITAL"},
  "mode": "fixed",
  "config": {
    "weights": {
      "trapped": {"sos": 0.35, "collapse": 0.30, "human_activity": 0.20, "smoke": 0.15},
      "passability": {"road_damage": 0.45, "fire_risk": 0.35, "congestion": 0.20, "drone_confidence": 0.00},
      "life_risk": {"fire": 0.40, "trapped_prob": 0.35, "time_urgency": 0.25},
      "priority": {"trapped_prob": 0.40, "life_risk": 0.30, "time_urgency": 0.20, "accessibility": 0.10},
      "utility": {"alpha": 0.30, "beta": 0.25, "gamma": 0.20, "delta": 0.15, "epsilon": 0.10},
      "astar_risk": {"fire": 0.35, "damage": 0.25, "congestion": 0.20, "secondary": 0.20}
    },
    "thresholds": {
      "car_min_passability": 0.45,
      "drone_recon_priority_risk": 0.70
    }
  },
  "zones": [],
  "roads": [],
  "units": [],
  "events": []
}
```

- `mode`
  - `fixed`：不可学习版
  - `learned`：可学习版
- `zones[]`

```json
{
  "zone_id": "A",
  "node_id": "ZONE_A",
  "observations": {
    "sos_signal": 0.90,
    "building_collapse": 0.85,
    "smoke": 0.75,
    "fire": 0.55,
    "road_damage": 0.60,
    "human_activity": 0.60,
    "congestion": 0.30,
    "time_urgency": 0.80,
    "drone_confidence": 0.00
  },
  "labels": {
    "trapped_ground_truth": 1,
    "priority_rank": 1
  }
}
```

- `roads[]`

```json
{
  "road_id": "R_HQ_A_1",
  "from": "HQ",
  "to": "ZONE_A",
  "distance": 6.0,
  "travel_time_base": 5.0,
  "status": "open",
  "risk": {
    "fire": 0.40,
    "damage": 0.60,
    "congestion": 0.20,
    "secondary_disaster": 0.10
  },
  "labels": {
    "passable_ground_truth": 0
  }
}
```

- `units[]`

```json
{
  "unit_id": "RescueCar-1",
  "type": "rescue_car",
  "start_node": "HQ",
  "speed": 1.2,
  "can_transport": true,
  "constraints": {
    "max_fire_risk": 0.70,
    "min_passability": 0.45
  }
}
```

- `events[]`

```json
{
  "event_id": "EVT_01",
  "event_type": "road_collapse",
  "trigger_step": 2,
  "target_id": "R_HQ_A_1",
  "changes": {"status": "blocked", "risk.damage": 1.0},
  "description": "A区南侧道路塌方"
}
```

## 可学习版数据方案

- 数据来源采用 `半人工合成数据`
- 训练集来源
  - 用规则自动生成 `100-300` 条区域/道路样本
  - 每条样本包含观测特征和标签
- 展示集来源
  - 基于项目书 A/B/C 场景扩展 `5-10` 个高质量案例
  - 用来展示固定版与学习版输出差异
- 标签设计
  - 区域级：`trapped_ground_truth`
  - 道路级：`passable_ground_truth`
  - 可选：`priority_rank`
- 训练目标
  - 学习 `trapped` 权重，使被困判断更接近标签
  - 学习 `passability` 权重，使道路可通行判断更接近标签
- 输出产物
  - `learned_weights.json`
  - 示例：

```json
{
  "trapped": {"sos": 0.38, "collapse": 0.27, "human_activity": 0.18, "smoke": 0.17},
  "passability": {"road_damage": 0.41, "fire_risk": 0.33, "congestion": 0.16, "drone_confidence": 0.10}
}
```

## 对外接口与输出

- 统一输出文件：`decision_output.json`
- 必须包含：
  - `run_mode`: `fixed` 或 `learned`
  - `zone_assessment[]`: `trapped_prob`, `passability_prob`, `life_risk`, `priority_score`
  - `assignments[]`: `unit_id`, `target_zone`, `mission_type`, `expected_utility`
  - `routes[]`: `unit_id`, `path`, `eta`, `path_risk`
  - `replan_log[]`: `trigger_event`, `old_plan`, `new_plan`, `reason`
  - `weights_used`: 本次实际使用的权重
- Qwen 和前端只消费输出 JSON，不直接参与计算。

## 明晚前排期

- `2026-06-15` 今晚
  - 定稿输入/输出 JSON
  - 做 `1` 份标准演示场景
  - 做 `100-300` 条训练样本生成规则
  - 跑通不可学习版的概率、风险、优先级
- `2026-06-16` 上午
  - 完成可学习版权重训练
  - 生成 `learned_weights.json`
  - 对比固定版与学习版的区域判断结果
- `2026-06-16` 下午
  - 接上任务分配与风险感知 A*
  - 支持 `road_collapse`、`drone_update`
  - 统一输出 `decision_output.json`
- `2026-06-16` 晚上前
  - 完成固定版与学习版的双结果演示
  - 准备你的讲解重点：固定参数 vs 学习参数、训练数据来源、效果对比、为什么仍然可解释

## 测试与验收

- 固定版
  - A 区高 SOS 高倒塌时，应有最高 `trapped_prob`
  - C 区高火灾低通行时，应优先无人机侦察
  - 道路塌方后，必须触发重规划
- 学习版
  - 能成功读入训练数据并输出 `learned_weights.json`
  - 用同一输入场景跑出一套不同于固定版的评分结果
  - 至少展示 1 个案例说明学习版比固定版更接近标签
- 最终验收
  - 同一个 `scenario_input.json` 可分别以 `fixed` 和 `learned` 模式运行
  - 两版输出结构一致
  - 所有结果都能口头解释字段来源和公式含义

## 假设与默认值

- 默认只做 `A/B/C` 三灾区、`2` 辆救援车、`1` 架无人机。
- 默认可学习版只学习概率层权重，不学习分配策略和路径策略。
- 默认训练数据来自规则生成加少量人工修正，不使用真实灾害数据库。
- 默认使用线性模型学习权重，优先保证可解释性和明晚前可交付。
